# Copy this file to `.env` and fill in real values. `.env` is gitignored.
#
# Gemini API key used by src/services/gemini.ts to generate the resource plan.
# Get one at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
